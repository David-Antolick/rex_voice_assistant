# Rex Voice Assistant - Environment Variables
# Copy to .env and fill in values (alternative to 'rex setup')

# YouTube Music Desktop (YTMD) Configuration
YTMD_TOKEN=
YTMD_HOST=localhost
YTMD_PORT=9863

# Spotify Configuration
SPOTIPY_CLIENT_ID=
SPOTIPY_CLIENT_SECRET=
SPOTIPY_REDIRECT_URI=http://127.0.0.1:8888/callback

# Optional: Override active service (ytmd, spotify, or none)
# REX_SERVICE=ytmd

# Optional: Override Whisper model
# REX_MODEL=small.en

# Optional: Override device (cuda or cpu)
# REX_DEVICE=cuda

# Optional: PulseAudio server address
# PULSE_SERVER=tcp:localhost:4713
